@model IEnumerable<mvc.Models.tblemployee>
@{
    ViewBag.Title = "show";
}

<h2>show</h2>

<table class="table table-hover table-striped table-responsive">
    <thead>
        <tr>
            <th> id</th>
            <th> firsta</th>
            <th>last</th>
            <th> add</th>
            <th>hone</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td> @Html.DisplayFor(obj => item.empid) </td>
            <td> @Html.DisplayFor(obj => item.firstname) </td>
        </tr>
        }
    </tbody>
</table>